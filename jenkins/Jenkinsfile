pipeline {
    agent any

    stages {
        stage('Unit Tests-Backend') {
            steps {
             dir ('bugtarker-backend') {
                go test -v ./...
            }
        }
    }
}